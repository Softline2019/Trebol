namespace SoftLine.Trebol.Application.Models.Product;

public static class ProductStatusLabel
{
    public const string ACTIVO = nameof(ACTIVO);
    public const string INACTIVO = nameof(INACTIVO);
}
